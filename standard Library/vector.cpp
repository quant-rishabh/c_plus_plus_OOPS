// 1. std::vector is a sequence container and also known as Dynamic Array or Array List.
// 2. Its size can grow and shrink dynamically, and no need to provide size at compile time.

// ELEMENT ACCESS
// at(), [], front(), back(), data()

// MODIFIERS:
// insert(), emplace(), push_back(), emplace_back(), pop_back(), resize(), swap(), erase(), clear()


// difference btw at and normal is at will give exception


// for optimization we just use reserve keyword to avoid any copy and new allocation 

#include <iostream>
using namespace std;

int main() {

  return 0;
}